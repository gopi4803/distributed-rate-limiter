spring.config.activate.on-profile=benchmark

############################################################
# Benchmark Rate Limiting Configuration
############################################################

rate-limiter.rules.demo.limit=${BENCHMARK_LIMIT:100000}
rate-limiter.rules.demo.window=${BENCHMARK_WINDOW:1m}
rate-limiter.rules.demo.algorithm=${BENCHMARK_ALGORITHM:TOKEN_BUCKET}